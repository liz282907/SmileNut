<% if @activity_added.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@activity_added.errors.count, "error") %>.
    </div>
    <ul>
    <% @activity_added.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>